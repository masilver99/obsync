{
  "name": "obsync-obsidian-plugin",
  "version": "0.1.0",
  "description": "Obsidian client for the Obsync self-hosted vault synchronization server.",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "esbuild": "0.25.5",
    "obsidian": "latest",
    "tslib": "^2.8.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
